name: publish-prod

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: Stable PEP 440 release tag to publish (for example, v1.2.3)
        required: true
        type: string

jobs:
  classify:
    name: classify release tag
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      classification: ${{ steps.version.outputs.classification }}
    env:
      RELEASE_TAG: ${{ inputs.tag || github.ref_name }}
    steps:
      - name: Checkout classification helper
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.tag && github.event.repository.default_branch || github.ref }}

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          version: "0.11.4"

      - name: Classify PEP 440 release tag
        id: version
        run: uv run --no-project --with packaging==26.0 python scripts/release_tag.py "$RELEASE_TAG"

  quality:
    name: validate, test, and build release
    needs: classify
    if: needs.classify.outputs.classification == 'stable'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      RELEASE_TAG: ${{ inputs.tag || github.ref_name }}
    steps:
      - name: Checkout release tag
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ inputs.tag && format('refs/tags/{0}', inputs.tag) || github.ref }}

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version-file: "pyproject.toml"

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          version: "0.11.4"

      - name: Install dependencies
        run: uv sync --frozen

      - name: Run ruff format check
        run: uv run --frozen ruff format --check .

      - name: Run ruff linting
        run: uv run --frozen ruff check .

      - name: Run type checking with pyright
        run: uv run --frozen pyright

      - name: Run tests
        run: uv run --frozen pytest --cov --cov-report=term-missing

      - name: Build distributions
        run: uv build

      - name: Verify built version and wheel installation
        run: |
          uv run --frozen python - <<'PY'
          import email
          import os
          import zipfile
          from pathlib import Path
          from packaging.version import Version

          wheels = list(Path("dist").glob("*.whl"))
          if len(wheels) != 1:
              raise SystemExit(f"expected exactly one wheel, found: {wheels}")
          with zipfile.ZipFile(wheels[0]) as archive:
              metadata_path = next(name for name in archive.namelist() if name.endswith(".dist-info/METADATA"))
              metadata = email.message_from_bytes(archive.read(metadata_path))
          tag_version = Version(os.environ["RELEASE_TAG"][1:])
          built_version = Version(metadata["Version"])
          if built_version != tag_version:
              raise SystemExit(f"built version {built_version} does not match tag version {tag_version}")
          PY
          python -m venv .wheel-venv
          .wheel-venv/bin/python -m pip install dist/*.whl
          .wheel-venv/bin/python -c "import flow_res"

      - name: Store verified distributions
        uses: actions/upload-artifact@v6
        with:
          name: release-distributions
          path: dist/
          if-no-files-found: error

  pypi-publish:
    name: publish release to PyPI
    needs: quality
    runs-on: ubuntu-latest
    environment:
      name: publish-prod
    permissions:
      actions: read
      id-token: write
    steps:
      - name: Download verified distributions
        uses: actions/download-artifact@v7
        with:
          name: release-distributions
          path: dist/

      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
